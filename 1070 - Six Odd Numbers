#include <stdio.h>
 
int main() {
 
    int x, i, impar;
    scanf("%d", &x);
    if(x % 2 == 0){
        impar = x + 1;
    }
    else{
        impar = x;
    }
    for(i = 0; i < 6; i++){
        printf("%d\n", impar);
        impar += 2;
    }
 
    return 0;
}
