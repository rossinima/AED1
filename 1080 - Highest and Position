#include <stdio.h>
 
int main() {
 
   int n, maior = 0, i, posicao = 0;
   for(i = 1; i <= 100; i++){
       scanf("%d", &n);
       if(maior < n){
           maior = n;
           posicao = i;
       }
   }
   printf("%d\n", maior);
   printf("%d\n", posicao);
 
    return 0;
}
