#include <stdio.h>

int main(void)
{
    int n;
    char str[100];

    scanf("%i", &n);
    getchar();  

    for (int i = 0; i < n; ++i)
    {
        fgets(str, sizeof(str), stdin);
        printf("Y\n");
    }

    return 0;
}
