#include <stdio.h>
 
int main() {
 
    int n, i, valor;
    scanf("%d", &n);
    for(i = 1; i <= 10; i++){
        valor = i * n;
        printf("%d x %d = %d\n", i, n, valor);
    }
 
    return 0;
}
