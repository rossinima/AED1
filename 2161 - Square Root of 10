#include <stdio.h>

int main(void)
{
    int n;
    double result = 0.0;  

    scanf("%i", &n);

    while (n--)
    {
        result = 1.0 / (result + 6.0);  
    }

    result += 3.0;  

    printf("%.10lf\n", result);
    return 0;
}
