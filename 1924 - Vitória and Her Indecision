#include <stdio.h>

int main(void)
{
    int n;
    char str[100];

    scanf("%i", &n);
    getchar(); // Consumir o newline deixado pelo scanf

    for (int i = 0; i < n; ++i)
    {
        fgets(str, sizeof(str), stdin);
    }

    printf("Ciencia da Computacao\n");

    return 0;
}
